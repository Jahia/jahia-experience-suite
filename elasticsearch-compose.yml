
es01:
  image: docker.elastic.co/elasticsearch/elasticsearch:7.17.20
  container_name: es01
  environment:
    - node.name=es01
    - cluster.name=prod-es-cluster
    - discovery.seed_hosts=es01,es02,es03
    - cluster.initial_master_nodes=es01,es02,es03
    - node.roles=master,data
    # Heap size (2GB)
    - ES_JAVA_OPTS=-Xms2g -Xmx2g
    # Memory lock
    - bootstrap.memory_lock=true
    # Recommended for small write load
    - indices.memory.index_buffer_size=20%
    # Network
    - network.host=0.0.0.0
    # Disable xpack security unless you request TLS config
    - xpack.security.enabled=false
  ulimits:
    memlock:
      soft: -1
      hard: -1
  volumes:
    - esdata01:/usr/share/elasticsearch/data
  ports:
    - "9200:9200"   # Expose only on es01 for clients
  networks:
    - esnet

es02:
  image: docker.elastic.co/elasticsearch/elasticsearch:7.17.20
  container_name: es02
  environment:
    - node.name=es02
    - cluster.name=prod-es-cluster
    - discovery.seed_hosts=es01,es02,es03
    - cluster.initial_master_nodes=es01,es02,es03
    - node.roles=master,data
    - ES_JAVA_OPTS=-Xms2g -Xmx2g
    - bootstrap.memory_lock=true
    - indices.memory.index_buffer_size=20%
    - network.host=0.0.0.0
    - xpack.security.enabled=false
  ulimits:
    memlock:
      soft: -1
      hard: -1
  volumes:
    - esdata02:/usr/share/elasticsearch/data
  networks:
    - esnet

es03:
  image: docker.elastic.co/elasticsearch/elasticsearch:7.17.20
  container_name: es03
  environment:
    - node.name=es03
    - cluster.name=prod-es-cluster
    - discovery.seed_hosts=es01,es02,es03
    - cluster.initial_master_nodes=es01,es02,es03
    - node.roles=master,data
    - ES_JAVA_OPTS=-Xms2g -Xmx2g
    - bootstrap.memory_lock=true
    - indices.memory.index_buffer_size=20%
    - network.host=0.0.0.0
    - xpack.security.enabled=false
  ulimits:
    memlock:
      soft: -1
      hard: -1
  volumes:
    - esdata03:/usr/share/elasticsearch/data
  networks:
    - esnet
